# Система достижений и бейджей

## Описание
Реализовать систему достижений (achievements/badges): игрок получает бейджи за различные действия в игре. Бейджи мотивируют исследовать весь контент, прокачивать разные навыки, играть в мини-игры. Бейджи отображаются в профиле игрока и на leaderboard.

## Definition of Ready (DOR)
- [x] Задача четко сформулирована
- [x] Определены критерии приемки
- [x] Оценена сложность (Story Points)
- [x] Нет блокирующих зависимостей
- [x] Определены необходимые ресурсы/ассеты

## Definition of Done (DOD)
- [ ] Код написан и работает
- [ ] Код соответствует стайлгайду
- [ ] Функционал протестирован вручную
- [ ] Нет консольных ошибок
- [ ] Документация обновлена (если требуется)
- [ ] Код прошел ревью (если требуется)

## Критерии приемки
1. `AchievementManager` (singleton) отслеживает и выдаёт достижения
2. Минимум 30 достижений разных категорий
3. При получении достижения — popup-уведомление (toast) с анимацией
4. Панель достижений доступна из HUD (список полученных и заблокированных)
5. Достижения сохраняются в SaveData
6. Скрытые достижения (hidden) — видны только после получения
7. Достижения с прогрессом (например, "Ответь на 100 вопросов" — 23/100)
8. Звуковой эффект при получении (если есть аудиосистема)

## Технические детали

### Типы: `src/types/achievements.ts`

```typescript
export type AchievementCategory =
  | 'career'       // карьерные достижения
  | 'assessment'   // ассесменты
  | 'social'       // мультиплеер / чат
  | 'minigame'     // мини-игры
  | 'exploration'  // исследование локаций
  | 'special'      // скрытые / пасхалки

export interface Achievement {
  id: string
  name: string
  description: string
  icon: string                    // emoji или sprite
  category: AchievementCategory
  hidden: boolean                 // скрытое до получения
  condition: AchievementCondition
  reward?: {
    stress?: number
    respect?: number
  }
}

export type AchievementCondition =
  | { type: 'flag'; flag: string }                           // флаг в GameState
  | { type: 'careerLevel'; pathId: string; level: string }   // достижение грейда
  | { type: 'domainScore'; domainId: string; minScore: number }
  | { type: 'assessmentCount'; count: number }               // пройдено N ассесментов
  | { type: 'miniGameScore'; gameId: string; minScore: number }
  | { type: 'miniGamePerfect'; gameId: string }              // макс. результат
  | { type: 'visitLocations'; count: number }                // посещено N локаций
  | { type: 'chatMessages'; count: number }                  // отправлено N сообщений
  | { type: 'itemsCollected'; count: number }
  | { type: 'custom'; checkFn: string }                      // id кастомной проверки

export interface AchievementProgress {
  achievementId: string
  unlocked: boolean
  unlockedAt?: number
  progress?: number   // для прогрессивных (23/100)
  total?: number
}
```

### Менеджер: `src/managers/Achievement.ts`

```typescript
export class AchievementManager {
  private static instance: AchievementManager

  checkAndUnlock(): void           // проверить все условия, разблокировать новые
  getUnlocked(): Achievement[]
  getLocked(): Achievement[]       // без hidden
  getProgress(id: string): AchievementProgress
  getAll(): Achievement[]

  // Подписка на Phaser-события для автоматической проверки
  private setupEventListeners(): void {
    this.game.events.on('assessmentAnswered', () => this.checkAndUnlock())
    this.game.events.on('careerLevelUp', () => this.checkAndUnlock())
    this.game.events.on('miniGameCompleted', () => this.checkAndUnlock())
    this.game.events.on('locationChanged', () => this.checkAndUnlock())
    // ...
  }
}
```

### Примеры достижений (30 штук)

**Career (6):**
- "Первые шаги" — выбрал карьерный путь
- "Middle ground" — достиг уровня Middle (любой путь)
- "Senior момент" — достиг Senior
- "На вершине" — достиг максимального грейда
- "Полиглот" — попробовал ассесменты из 3 разных путей
- "Jack of all trades" (hidden) — набрал 30+ баллов в доменах 3 разных путей

**Assessment (6):**
- "Первый ответ" — прошёл первый ассесмент
- "Десяточка" — ответил на 10 вопросов
- "Сотня" — ответил на 100 вопросов (прогрессивное)
- "Перфекционист" — получил 100 баллов за вопрос
- "Серия" — 5 ответов подряд с score >= 80
- "Эрудит" (hidden) — набрал 70+ в 5 доменах одного пути

**Social (5):**
- "Привет, мир!" — отправил первое сообщение в чат
- "Болтун" — 50 сообщений (прогрессивное)
- "Кофе-брейк" — провёл 5 минут на кухне с другими игроками
- "Нетворкер" — встретил 10 разных игроков
- "Тусовщик" (hidden) — 3 игрока одновременно на кухне

**Minigame (6):**
- "Геймер" — сыграл в первую мини-игру
- "Коллекционер" — сыграл во все мини-игры
- "Мастер Code Review" — 3 звезды в Code Review
- "Архитектор" — 3 звезды в Architecture Puzzle
- "Scrum Master" — 3 звезды в Sprint Planning
- "Arcade King" (hidden) — 3 звезды во всех мини-играх

**Exploration (4):**
- "Турист" — посетил все локации
- "Коллега" — поговорил со всеми NPC
- "Кладоискатель" — собрал все предметы
- "Детектив" (hidden) — нашёл секретные документы

**Special (3):**
- "Антистресс" — снизил стресс с 80+ до 20
- "Дзен" (hidden) — стресс = 0
- "Кофеман" — выпил 10 кофе (прогрессивное)

### Popup-уведомление

При получении:
```
+----------------------------+
| 🏆 Достижение разблокировано!|
|                              |
| "Перфекционист"              |
| Получил 100 баллов за вопрос |
|                              |
| Награда: Respect +5          |
+----------------------------+
```

- Появляется сверху экрана с slide-down анимацией
- Исчезает через 3 секунды с fade-out
- Можно кликнуть для открытия панели достижений

### Панель достижений

- Открывается из HUD (кнопка "Трофеи" / "🏆")
- Grid-layout: иконка + название + описание
- Цветные = разблокированные, серые = заблокированные
- Hidden показывается как "???" до разблокировки
- Прогрессивные показывают прогресс-бар
- Счётчик: "17/30 достижений"

### Сохранение

Расширить `SaveData`:
```typescript
achievements?: AchievementProgress[]
```

## Зависимости
- Нет жёстких зависимостей (работает с любым набором систем)
- Мягкие: мультиплеер (для social-достижений), мини-игры (для minigame-достижений)

## Оценка
- Story Points: 8
- Приоритет: Low

## Метки
- `feature`, `gameplay`, `ui`
