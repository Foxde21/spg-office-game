# Чат-система (кухня, чат-бабблы, общение игроков)

## Описание
Реализовать чат между игроками: текстовые сообщения отображаются как "бабблы" над аватарами в игровом мире. Кухня — особая социальная зона, где чат работает расширенно (история сообщений, больше пространства). Чат интегрируется с мультиплеером через WebSocket.

## Definition of Ready (DOR)
- [x] Задача четко сформулирована
- [x] Определены критерии приемки
- [x] Оценена сложность (Story Points)
- [x] Нет блокирующих зависимостей
- [x] Определены необходимые ресурсы/ассеты

## Definition of Done (DOD)
- [ ] Код написан и работает
- [ ] Код соответствует стайлгайду
- [ ] Функционал протестирован вручную
- [ ] Нет консольных ошибок
- [ ] Документация обновлена (если требуется)
- [ ] Код прошел ревью (если требуется)

## Критерии приемки
1. Игрок может отправить сообщение нажав Enter (или кнопку "Chat")
2. Сообщение отображается как бабл над аватаром отправителя (для всех в локации)
3. Бабл исчезает через 5 секунд (с fade-анимацией)
4. На кухне — расширенный чат: панель справа с историей сообщений (последние 50)
5. Сообщения привязаны к локации — видят только игроки в той же локации
6. Имя отправителя и его грейд отображаются в чате
7. Максимальная длина сообщения — 200 символов
8. Фильтр нецензурных слов (базовый) — опционально для MVP
9. Чат работает только в мультиплеере; в single-player кнопка чата скрыта

## Технические детали

### WebSocket-события (расширение 020)

```typescript
// Клиент → Сервер
'chat:message' → { text: string, location: string }

// Сервер → Клиенты (только в той же локации)
'chat:message' → { playerId: string, playerName: string, text: string, timestamp: number }
```

Сервер фильтрует: сообщение рассылается только игрокам в той же локации.

### Chat Bubble — game object

```typescript
export class ChatBubble extends Phaser.GameObjects.Container {
  private background: Phaser.GameObjects.Graphics
  private text: Phaser.GameObjects.Text
  private parent: Phaser.GameObjects.Container  // Player или RemotePlayer

  constructor(scene: Phaser.Scene, parent: Container, message: string)

  show(): void  // появляется с scale-анимацией
  hide(): void  // исчезает через 5с с fade
}
```

- Позиция: над головой аватара (offset y: -60)
- Фон: белый скруглённый прямоугольник с "хвостиком" (как в комиксах)
- Текст: тёмный, макс 3 строки, word-wrap
- При новом сообщении предыдущий бабл заменяется

### Кухня — расширенный чат

На локации `kitchen` дополнительно отображается панель чата:

```
+------------------------------------------+
|  Кухня                                    |
|                                            |
|  [Игровое поле]          | Чат            |
|                           | ------------- |
|  🧑 Player1              | Player2: Привет|
|     💬 "Привет!"         | Player1: Хей!  |
|                           | Player3: Кофе? |
|  🧑 Player2              |                |
|     💬 "Хей!"            |                |
|                           |                |
|                           | [Ввод...]  [→] |
+------------------------------------------+
```

- Панель справа: ширина 250px, полупрозрачный фон
- Скролл истории сообщений (последние 50)
- Каждое сообщение: `[имя]: текст`
- Цвет имени — по грейду (junior: белый, senior: зелёный, architect: фиолетовый)

### UI ввода сообщения

- **Триггер**: нажатие Enter или кнопка "Chat" в HUD
- Появляется текстовое поле внизу экрана (как dialogue input, уже есть подобная механика для AI-диалогов)
- Enter — отправить, Escape — отменить
- Во время набора — индикатор "печатает..." для других игроков (опционально)

### Бонус кухни: снижение стресса через общение

Игровая механика: пребывание на кухне с другими игроками снижает стресс:
- Каждые 30 секунд на кухне, если есть хотя бы 1 другой игрок: stress -2
- Отправка сообщения на кухне: stress -1 (социализация!)
- Это мотивирует использовать кухню как social hub

### Интеграция

- `MultiplayerManager` расширяется методами `sendChatMessage(text)`, `onChatMessage(callback)`
- `GameScene` создаёт ChatBubble при получении сообщения
- `UIScene` управляет вводом текста и панелью чата на кухне

## Зависимости
- 020-multiplayer-infrastructure (WebSocket)

## Оценка
- Story Points: 8
- Приоритет: Medium

## Метки
- `feature`, `gameplay`, `ui`
