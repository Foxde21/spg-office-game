# Leaderboard + профили игроков

## Описание
Реализовать серверный leaderboard и профили игроков. Leaderboard показывает рейтинг по карьерным путям, общий score, достижения. Профиль игрока: аватар, грейд, статистика, бейджи. Доступно через панель в UI и на доске объявлений в Open Space.

## Definition of Ready (DOR)
- [x] Задача четко сформулирована
- [x] Определены критерии приемки
- [x] Оценена сложность (Story Points)
- [x] Нет блокирующих зависимостей
- [x] Определены необходимые ресурсы/ассеты

## Definition of Done (DOD)
- [ ] Код написан и работает
- [ ] Код соответствует стайлгайду
- [ ] Функционал протестирован вручную
- [ ] Нет консольных ошибок
- [ ] Документация обновлена (если требуется)
- [ ] Код прошел ревью (если требуется)

## Критерии приемки
1. Серверный API для leaderboard (`/api/leaderboard`)
2. Профиль игрока с аватаром, грейдом, статистикой
3. Leaderboard: общий рейтинг + фильтр по карьерному пути
4. Top-10 отображается в UI-панели
5. Доска объявлений в Open Space — интерактивный объект, показывает leaderboard
6. При клике на игрока в leaderboard — его профиль
7. Профиль: карьерный путь, грейд, домены (radar chart), достижения, мини-игры рекорды
8. Обновляется при каждом ассесменте / достижении

## Технические детали

### Серверный API: `server/routes/leaderboard.ts`

```typescript
// GET /api/leaderboard?path=ai&limit=10
// Возвращает top игроков

// GET /api/profile/:playerId
// Возвращает профиль игрока

// POST /api/profile/update
// Обновляет данные игрока (при сохранении)
```

### Хранение на сервере

Для MVP — in-memory store (JSON файл):
```typescript
interface LeaderboardEntry {
  playerId: string
  playerName: string
  careerPath: string
  careerLevel: string
  averageScore: number
  totalAssessments: number
  achievementCount: number
  miniGameBestScores: Record<string, number>
  lastActive: number
}
```

В будущем — миграция на SQLite / PostgreSQL.

### Клиентская часть

**LeaderboardPanel** в UIScene:
```
+-------------------------------------------+
|  🏆 Leaderboard         [All] [AI] [Eng]  |
+-------------------------------------------+
|  #  | Имя        | Путь    | Грейд   | ⭐ |
+-------------------------------------------+
|  1  | Alice      | AI      | Senior  | 82 |
|  2  | Bob        | Eng     | Middle  | 71 |
|  3  | Charlie    | Product | Senior  | 68 |
|  4  | Вы ← тут  | AI      | Junior  | 45 |
|  ...                                       |
+-------------------------------------------+
|            [Мой профиль]                   |
+-------------------------------------------+
```

**Профиль игрока:**
```
+-------------------------------------------+
|  👤 Alice                                  |
|  AI Senior (score: 82)                    |
|                                            |
|  [Radar Chart — домены]                   |
|                                            |
|  🏆 Достижения: 17/30                     |
|  🎮 Лучший в Code Review: 920            |
|  📊 Ассесментов: 47                       |
|  ⏱ Времени в игре: 4ч 22м                |
+-------------------------------------------+
```

### Доска объявлений в Open Space

Интерактивный объект на локации Open Space:
- При клике: открывает LeaderboardPanel
- Визуал: доска с "бумажками" — top-3 имени видны на доске прямо в игровом мире
- Обновляется при входе в локацию

### Radar Chart (повторное использование)

Если реализовано в задаче 013 (Skill Tree) — переиспользовать компонент для профиля.

### Отправка данных на сервер

При каждом авто-сохранении (SaveManager) — отправлять обновлённые данные:
```typescript
// В SaveManager.save()
if (MultiplayerManager.getInstance().isConnected()) {
  fetch('/api/profile/update', {
    method: 'POST',
    body: JSON.stringify(leaderboardData)
  })
}
```

## Зависимости
- 020-multiplayer-infrastructure (серверная часть, подключение)
- 029-achievements-system (для отображения в профиле)

## Оценка
- Story Points: 8
- Приоритет: Low

## Метки
- `feature`, `ui`, `gameplay`
