# Мини-игра: Code Review Challenge

## Описание
Мини-игра в аркадном автомате: игроку показывается фрагмент кода с багами/проблемами. Нужно найти и кликнуть на строки с ошибками за ограниченное время. Чем больше багов нашёл и чем быстрее — тем больше очков. Прокачивает "глаз" на код-ревью, актуально для Engineering, QA и AI путей.

## Definition of Ready (DOR)
- [x] Задача четко сформулирована
- [x] Определены критерии приемки
- [x] Оценена сложность (Story Points)
- [x] Нет блокирующих зависимостей
- [x] Определены необходимые ресурсы/ассеты

## Definition of Done (DOD)
- [ ] Код написан и работает
- [ ] Код соответствует стайлгайду
- [ ] Функционал протестирован вручную
- [ ] Нет консольных ошибок
- [ ] Документация обновлена (если требуется)
- [ ] Код прошел ревью (если требуется)

## Критерии приемки
1. Extends `MiniGameScene` — использует framework из 022
2. Показывается фрагмент кода (10-20 строк) с подсветкой синтаксиса (цветом)
3. 3-5 строк содержат баги (отмечены в data, не видны игроку)
4. Клик на строку с багом → +100 очков, строка подсвечивается зелёным, показывается объяснение
5. Клик на чистую строку → -50 очков, строка подсвечивается красным
6. Таймер: 60 секунд
7. 5 раундов (5 фрагментов кода за игру)
8. Минимум 10 фрагментов кода в банке (чтобы не повторялись)
9. Фрагменты на разных языках (TypeScript, Python, SQL)
10. Результат: звёзды (1-3) по количеству найденных багов

## Технические детали

### Scene: `src/scenes/minigames/CodeReviewGame.ts`

```typescript
export class CodeReviewGame extends MiniGameScene {
  getGameId(): string { return 'code-review' }
  getGameName(): string { return 'Code Review Challenge' }
  getInstructions(): string { return 'Найди баги в коде! Кликай на строки с ошибками.' }
  getMaxScore(): number { return 2500 } // 5 раундов × 5 багов × 100 очков
}
```

### Data: `src/data/miniGames/codeReviewSnippets.ts`

```typescript
export interface CodeSnippet {
  id: string
  language: 'typescript' | 'python' | 'sql'
  title: string  // "Функция обработки заказов"
  lines: CodeLine[]
}

export interface CodeLine {
  code: string
  hasBug: boolean
  bugExplanation?: string  // "Off-by-one error: должно быть < length, не <= length"
}
```

**Примеры фрагментов:**

1. **TypeScript — Off-by-one:**
```typescript
function processItems(items: Item[]) {
  for (let i = 0; i <= items.length; i++) {  // BUG: <= should be <
    console.log(items[i].name)
  }
}
```
bugExplanation: "Off-by-one: `i <= items.length` выйдет за границы массива. Должно быть `i < items.length`."

2. **Python — Mutable default argument:**
```python
def add_item(item, items=[]):  # BUG: mutable default
    items.append(item)
    return items
```
bugExplanation: "Мутабельный аргумент по умолчанию: список `items=[]` разделяется между вызовами. Используй `items=None`."

3. **SQL — SQL injection:**
```sql
SELECT * FROM users
WHERE name = '${userName}'  -- BUG: SQL injection
ORDER BY created_at DESC
```
bugExplanation: "SQL injection: подставлять переменные напрямую в SQL опасно. Используй параметризованные запросы."

4. **TypeScript — Race condition / missing await:**
```typescript
async function saveUser(user: User) {
  const exists = checkUserExists(user.id)  // BUG: missing await
  if (exists) {
    return updateUser(user)
  }
  return createUser(user)
}
```

5. **Python — Comparison instead of assignment:**
```python
def update_status(order):
    if order.status == 'pending':
        order.status == 'processing'  # BUG: == instead of =
        order.save()
```

### UI внутри мини-игры

```
+------------------------------------------+
|  Code Review Challenge    Score: 350      |
|  Раунд: 2/5             ⏱ 0:42          |
+------------------------------------------+
|  📄 user_service.py                      |
|                                           |
|  1  def get_user(user_id):               |
|  2    user = db.query(User)              |
|  3      .filter(User.id == user_id)      |
|  4      .first()                         |
|  5    if user == None:          🔍       |
|  6      raise NotFoundError()            |
|  7    user.last_login = datetime.now     |
|  8    db.commit()                        |
|  9    return user                        |
| 10                                       |
+------------------------------------------+
|  Найдено: 1/3 багов   [Пропустить →]    |
+------------------------------------------+
```

- Строки кликабельны
- При наведении — подсветка
- Клик на баг → зелёная рамка + popup с объяснением (1.5 сек)
- Клик на чистую → красная вспышка
- Счётчик найденных багов внизу
- Кнопка "Пропустить" для перехода к следующему раунду

### Награды

- 3 звезды (>80% багов): stress -10, respect +3
- 2 звезды (>50%): stress -7, respect +1
- 1 звезда (>20%): stress -5

## Зависимости
- 022-game-room-minigame-framework (MiniGameScene, framework)

## Оценка
- Story Points: 5
- Приоритет: Medium

## Метки
- `feature`, `gameplay`
